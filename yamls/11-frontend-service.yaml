apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: kubecoin
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30143
  selector:
    module: frontend