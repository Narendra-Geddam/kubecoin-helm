apiVersion: v1
kind: Service
metadata:
  name: database-replica-svc
  namespace: kubecoin
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    module: db-postgres
    role: replica