apiVersion: v1
kind: Service
metadata:
  name: database-primary-svc
  namespace: kubecoin
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    module: db-postgres
    role: primary